sudo apt update
sudo apt install -y build-essential libpcap-dev libpcre3-dev libdumbnet-dev liblzma-dev libssl-dev zlib1g-dev
ip a
sudo apt-get install snort
snort -T -c /etc/snort/snort.conf
sudo chmod 644 /etc/snort/snort.conf
sudo chown root:root /etc/snort/snort.conf
sudo chmod 755 /var/log/snort
sudo chown snort:snort /var/log/snort
sudo chmod 644 /var/log/snort/snort.alert.fast
sudo chown snort:snort /var/log/snort/snort.alert.fast
sudo systemctl restart snort
snort -T -c /etc/snort/snort.conf
ifconfig
sudo vi /etc/udev/rules.d/70-persistent-net.rules
sudo nano /etc/udev/rules.d/70-persistent-net.rules
sudo udevadm control --reload-rules
sudo udevadm trigger
sudo rebot
sudo reboot
ifconfig
sudo apt-get install snort
sudo chmod 644 /etc/snort/snort.conf
sudo chown root:root /etc/snort/snort.conf
sudo chmod 755 /var/log/snort
sudo chown snort:snort /var/log/snort
sudo chmod 644 /var/log/snort/snort.alert.fast~
sudo chmod 644 /var/log/snort/snort.alert.fast
sudo chown snort:snort /var/log/snort/snort.alert.fast
sudo systemctl restart snort
sudo rm -rf /usr/local/src/snort-2.9.20
sudo rm -rf /usr/local/lib/libdaq_static*
sudo rm -rf /usr/local/include/daq
sudo rm -rf /usr/local/include/snort
sudo rm -f /usr/local/bin/snort
sudo rm -f /usr/local/bin/snortrules-snapshot-*
sudo rm -f /usr/local/bin/snort
sudo rm -f /usr/local/bin/snortrules-snapshot-*
sudo apt-get remove --purge libdaq-dev libpcap-dev libpcre3-dev
sudo apt-get autoremove
sudo apt-get purge snort
sudo apt-get install snort
sudo nano /etc/snort/snort.conf
snort -T -c /etc/snort/snort.conf
ERROR: /etc/snort/snort.conf(0) Unable to open rules file "/etc/snort/snort.con
sudo snort -T -c /etc/snort/snort.conf
sudo systemctl restart snort
sudo systemctl start snort
sudo systemctl status snort
git config --global user.name
sudo apt install git
sudo apt update
git config --global user.name "dlwldls1046"
git config --global user.email "dlwldls1046@naver.com"
cd /path/to/your/project
ssh-keygen -t rsa -b 4096 -C "dlwldls1046@naver.com"
cat ~/.ssh/id_rsa.pub
ssh -T git@github.com
git clone git@github.com:leeazyone/SnortIDS.git
cd SnortIDS
git status
git add .
git commit -m "hello"
git push origin master
mkdir SnortIDS  # 프로젝트 폴더 생성
cd SnortIDS     # 해당 폴더로 이동
git init        # Git 저장소 초기화
git remote add origin https://github.com/leeazyone/SnortIDS.git
git add .        # 모든 파일 추가
git commit -m "Initial commit"   # 커밋 메시지 작성
git push -u origin master
git push -u origin main
git branch
git fetch origin
git checkout -b main origin/main
git push -u origin main
git remote -v
git remote set-url origin git@github.com:leeazyone/SnortIDS.git
ssh -T git@github.com
git push -u origin main
echo "This is the first file" > first
ls
git add first
git commit -m "Add first file"
git push origin main
apt-get install wireshark
su -
apt-get install wireshark
su -
exit
su -
apt-get install wireshark
sudo 
su -
sudo root
sudo passwd root
sudo -
su -
snort -v
sudo find /-name snort
sudo snort -v
git status
ip a
ifconfig
sudo vi /etc/udev/rules.d/70-persistent-net.rules
sudo udevadm control --reload-rules
sudo udevadm trigger
sudo reboot
ifconfig
sudo vi /etc/udev/rules.d/70-persistent-net.rules
[200~sudo vi /etc/udev/rules.d/70-persistent-net.rules
~sudo vi /etc/udev/rules.d/70-persistent-net.rules
sudo vi /etc/udev/rules.d/70-persistent-net.rules
sudo udevadm control --reload-rules
sudo udevadm trigger
sudo reboot
